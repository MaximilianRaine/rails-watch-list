<%= simple_form_for [@list, @bookmark], class: 'mt-4' do |f| %>
  <%= f.input :comment, label: 'Comment' %>
  <%= f.association :movie, collection: Movie.all, prompt: 'Select a movie', label: 'Movie' %>
  <%= f.submit 'Add Bookmark', class: 'btn btn-success' %>
<% end %>
